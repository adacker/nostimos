# Node / build artifacts
node_modules/
dist/
build/
*.tsbuildinfo
.DS_Store

# Local SQLite database + uploads (local-first data, not source)
packages/server/data/
*.db
*.db-journal
*.db-wal

# Env
.env
.env.local

# Local editor / harness settings
.claude/settings.local.json

# Generated rebar artifacts (regenerated by scripts/steward.sh on each run)
STEWARD_REPORT.md
architecture/.state/*.json

# ASK runtime artifacts
agents/*/inbox/
agents/*/outbox/
agents/*/.pid
agents/*/.last-stderr
agents/*/.progress
agents/*/.session-id
agents/*/memory.log.md
shared/messages/events.jsonl
